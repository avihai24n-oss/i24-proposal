# i24NEWS proposal

A three slide pitch deck for i24NEWS, built by TodoAI Studio.

`index.html` is self contained: every image, logo and icon is embedded,
so it opens offline and needs no build step.

Navigate with the arrow keys, by clicking the left and right thirds of
the frame, or by swiping.
